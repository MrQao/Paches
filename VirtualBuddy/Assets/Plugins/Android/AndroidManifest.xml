<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unity3d.player"
    android:installLocation="auto">

	<!-- 🔌 网络 / 状态 / 录音（语音识别） -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />

	<!-- 🥽 Quest 系列所需能力（OpenXR/头部追踪） -->
	<uses-feature
        android:name="android.hardware.vr.headtracking"
        android:version="1"
        android:required="true" />

	<application
        android:label="@string/app_name"
        android:icon="@mipmap/app_icon"
        android:allowBackup="false"
        tools:targetApi="31">

		<!-- 如果需要访问 http 明文流量（非 https），取消下一行注释 -->
		<!-- android:usesCleartextTraffic="true" -->

		<activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@style/UnityThemeSelector"
            android:configChanges="locale|fontScale|keyboard|keyboardHidden|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:exported="true">

			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
				<!-- Quest VR 启动分类 -->
				<category android:name="com.oculus.intent.category.VR" />
			</intent-filter>

			<!-- Unity 活动标记（保留） -->
			<meta-data android:name="unityplayer.UnityActivity" android:value="true" />
			<!-- 保持焦点感知，避免被系统抢焦点 -->
			<meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
		</activity>

		<!-- 安装时权限弹窗（正常/运行时权限仍遵循 Android 规则） -->
		<meta-data android:name="unityplayer.SkipPermissionsDialog" android:value="false" />

		<!-- Oculus/Quest 相关元数据 -->
		<meta-data android:name="com.samsung.android.vr.application.mode" android:value="vr_only" />
		<meta-data android:name="com.oculus.ossplash.background" android:value="black" />
		<meta-data android:name="com.oculus.telemetry.project_guid" android:value="2b44f74b-f8b2-436f-9dcb-701d77e7af7e" />
		<meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest|quest2|questpro|quest3|quest3s"
            tools:replace="android:value" />
	</application>
</manifest>
